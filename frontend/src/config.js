export const backend = {
  protocol: 'http',
  ip: 'localhost',
  port: 8080,
}
export const defaultHeaders = {
  headers: {
    'Accept': 'application/json',
    'Content-Type': 'application/json'
  },
}

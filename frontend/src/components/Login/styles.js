import { StyleSheet } from 'aphrodite'


export const styles = StyleSheet.create({
  popupHeader: {
    textAlign: 'center',
  },
})

export const muiStyles = {
  snackbar: {
    hideDuration: 2000,
  },
}
